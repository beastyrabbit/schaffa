pre-commit:
  parallel: true
  commands:
    biome:
      run: pnpm biome check .
    gitleaks:
      run: gitleaks git --pre-commit --staged --redact --no-banner

pre-push:
  commands:
    quality:
      run: pnpm check
